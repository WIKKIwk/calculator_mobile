name: calculator_app
description: "Mahsulot hisoblagich — Flutter + GraphQL + Golang"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  graphql_flutter: ^5.2.0-beta.8
  intl: ^0.20.2
  device_preview: ^1.2.0
  hive_ce: ^2.19.3
  flutter_secure_storage: ^10.0.0
  cryptography: ^2.9.0
  share_plus: ^12.0.2
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
