name: calculator_offline
description: "Mahsulot hisoblagich — faqat offline (server va baza talab qilmaydi)"
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  device_preview: ^1.2.0
  hive_ce: ^2.19.3
  path_provider: ^2.1.5
  flutter_secure_storage: ^10.0.0
  cryptography: ^2.9.0
  share_plus: ^12.0.2
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
